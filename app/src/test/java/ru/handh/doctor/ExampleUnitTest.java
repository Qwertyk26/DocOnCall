package ru.handh.doctor;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

/**
 * To work on unit tests, switch the Test Artifact in the Build Variants view.
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() throws Exception {
        assertEquals(4, 2 + 2);
    }

    @Test
    public void addition_isCorrect2() throws Exception {


//        Activity activity = Robolectric.setupActivity(LoginActivity.class);

//        Button button = (Button) activity.findViewById(R.id.loginEnter_button);
//        TextView results = (TextView) activity.findViewById(R.id.loginEmail);
//
//        button.performClick();
//        assertThat(results.getText().toString(), equalTo("111"));


//        assertEquals(4, 2 + 2);
    }
}